#include <stdio.h>

int main()
{
 int n, m, i, * ptr;
 printf("enter n:");
 scanf("%d", &n);
 ptr=malloc(n* sizeofint());
 printf("enter m");
 scanf("%d", &m);
 int a[m], b;
 for( i=0; i<m; i++);
 {
   b= a[i];
 }
 
 print("result:%d", b);
 return 0;
}
