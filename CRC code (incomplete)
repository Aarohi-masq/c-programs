#include<stdio.h>
int main()
{
    int m,n;
    scanf("%d%d", &n,&m);
    int a[n+m-1], data[n], divi[m], rem[m-1];
    for(int i=0; i<n; i++)
    {
        scanf("%d", &data[i]);
    }
    for (int i=0; i<m; i++)
    {
        scanf("%d", & divi[i]);
    }
    for(int i=0; i<n; i++)
    {
        a[i]=div[i];
    }
    for(int i=n; i<n+m-1; i++)
    {
        a[i]=0;
    }
    for(int i=0; i<n+m-1; i++)
    {
        if(data[i]==div[i])
        {
            rem[i]=0;
        }
        else rem=1;
    }
}
