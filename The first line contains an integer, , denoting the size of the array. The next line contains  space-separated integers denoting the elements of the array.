#include<stdio.h>
int main()
{
    int n,sum=0;
    scanf("%d",&n);
    int a[n],s[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
    {
        s[n-1-i]=a[i];
    }
    for(int i=0;i<n;i++)
    {
        printf("%d ",s[i]);
    }
    return 0;
}
